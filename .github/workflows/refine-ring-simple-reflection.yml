name: Refine RingContainer simple reflection
on:
  push:
    paths:
      - .github/workflows/refine-ring-simple-reflection.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply simple reflection update
        run: python .github/scripts/refine-ring-simple-reflection.py
      - name: Syntax check
        run: node --check RingContainer.js
      - name: Targeted checks
        run: |
          grep -n 'static #isSimpleLength' RingContainer.js
          grep -n 'var\\(' RingContainer.js
          grep -n ': "calculated"' RingContainer.js
      - name: Commit and cleanup
        run: |
          rm -f .github/workflows/refine-ring-simple-reflection.yml
          rm -f .github/scripts/refine-ring-simple-reflection.py
          git config user.name github-actions
          git config user.email github-actions@users.noreply.github.com
          git add RingContainer.js .github
          git commit -m 'Preserve simple RingContainer length attributes'
          git push
