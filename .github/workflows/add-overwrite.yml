name: Add ClockTimer overwrite
on:
  push:
    paths:
      - '.github/workflows/add-overwrite.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Apply patch
        run: python .github/scripts/add-overwrite.py
      - name: Check syntax
        run: node --check ClockTimer.js
      - name: Commit and self-clean
        run: |
          rm .github/workflows/add-overwrite.yml
          rm .github/scripts/add-overwrite.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add ClockTimer.js .github/workflows/add-overwrite.yml .github/scripts/add-overwrite.py
          git commit -m "Add async ClockTimer overwrite"
          git push origin HEAD:main
